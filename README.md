# passgen —— 随机密码/口令生成器

命令行小工具，生成随机密码或验证码。用 Go 标准库写，没引第三方库。

`math/rand` 是伪随机、可被预测，不适合做密码。本工具用 `crypto/rand`（系统熵池）取随机下标。

## 编译与运行

```powershell
go run .                 # 默认：1 条 16 位、含大小写+数字+符号
go run . -len 24 -count 5
go run . -no-upper       # 不要大写
go run . -no-symbols     # 不要符号
go run . -only-digits    # 只出数字（验证码）
```

## 参数

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `-len` | 16 | 密码长度 |
| `-count` | 1 | 一次生成几条 |
| `-symbols` | true | 是否包含符号 |
| `-no-upper` | false | 不包含大写字母 |
| `-only-digits` | false | 只生成数字 |
| `-no-upper` | false | 不包含大写字母（小写/数字/符号仍每类必含） |

## 真实随机 & 每类必含

- 用 `crypto/rand`（系统熵池）取随机下标，比 `math/rand` 伪随机更安全。
- 生成的密码**保证每个字符类（小写 / 大写 / 数字 / 符号）至少出现一次**，再用 Fisher–Yates 原地打乱，避免某些类完全抽不到的情况（普通纯随机可能整条都是小写）。
- 强度按**真实信息熵**估算：`长度 × log2(字符集大小)` 位，比粗略的"长度×种类数"更准确。

## 示例输出

```text
aZ3$kL9@qW2#mN7p  [强, 95.3 bits]
```

方括号里依次是强度等级与估算熵位（仅供参考，非严谨评分）。

## 文件结构

```
passgen/
├── go.mod        # 模块定义（无任何第三方依赖）
├── main.go       # 全部逻辑：随机取字符 + 每类必含 + 熵估算
└── main_test.go  # 单元测试：长度/字符集/每类必含/熵/强度/字符集拼接
```
